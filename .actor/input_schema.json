{
  "title": "Michael Page Jobs Scraper",
  "description": "Scrape job listings from Michael Page with fast pagination and rich job-detail extraction, including full descriptions and structured fields.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Start scraping from a specific Michael Page search URL. If provided, this overrides keyword and location.",
      "editor": "textfield"
    },
    "keyword": {
      "title": "Keyword",
      "type": "string",
      "description": "Job search keyword(s), e.g., 'Software Engineer', 'Project Manager', 'Data Analyst'.",
      "editor": "textfield",
      "prefill": "project manager"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Location filter for Michael Page jobs, e.g., 'New York', 'London'.",
      "editor": "textfield"
    },
    "results_wanted": {
      "title": "Maximum number of jobs",
      "type": "integer",
      "description": "The maximum number of jobs to collect. Leave empty to collect all available jobs.",
      "minimum": 1,
      "default": 20,
      "prefill": 20,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum number of pages",
      "type": "integer",
      "description": "A safety cap on the number of search result pages to visit.",
      "minimum": 1,
      "editor": "number",
      "default": 10,
      "prefill": 10
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Use Apify Proxy for reliable scraping. Residential proxies recommended.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    }
  },
  "required": []
}
